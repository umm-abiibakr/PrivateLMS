@model IEnumerable<PrivateLMS.ViewModels.LoanViewModel>
@{
    ViewData["Title"] = "My Loans";
}

<h2 class="text-center mb-4">My Loans</h2>

@if (!Model.Any())
{
    <p class="text-center">You have no active loans.</p>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Loan ID</th> <!-- Added -->
                <th>Book Title</th>
                <th>Loan Date</th>
                <th>Due Date</th>
                <th>Return Date</th>
                <th>Renewed</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var loan in Model)
            {
                <tr>
                    <td>@loan.LoanRecordId</td> <!-- Added -->
                    <td>@loan.BookTitle</td>
                    <td>@loan.LoanDate.ToShortDateString()</td>
                    <td>
                        @(loan.DueDate?.ToShortDateString() ?? "N/A")
                        @if (loan.DueDate.HasValue && loan.DueDate < DateTime.Now && !loan.ReturnDate.HasValue)
                        {
                            <span class="badge bg-danger ms-2">Overdue</span>
                        }
                    </td>
                    <td>
                        @if (loan.ReturnDate.HasValue)
                        {
                            @loan.ReturnDate.Value.ToShortDateString()
                        }
                        else
                        {
                            <span class="badge bg-warning">Not Returned</span>
                        }
                    </td>
                    <td>@(loan.IsRenewed ? "Yes" : "No")</td>
                    <td>
                        @if (!loan.ReturnDate.HasValue)
                        {
                            <a asp-action="Return" asp-route-loanRecordId="@loan.LoanRecordId" class="btn btn-primary btn-sm">Return</a>
                            @if (!loan.IsRenewed)
                            {
                                <a asp-action="Renew" asp-route-id="@loan.LoanRecordId" class="btn btn-success btn-sm">Renew</a>
                            }
                        }
                        <a asp-controller="Fines" asp-action="MyFines" class="btn btn-info btn-sm">View Fines</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

<div class="text-end">
    <a asp-controller="Books" asp-action="Index" class="btn btn-secondary">Back to Books</a>
</div>